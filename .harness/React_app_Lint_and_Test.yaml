pipeline:
  name: "React app - Lint and Test "
  identifier: React_app_Lint_and_Test
  projectIdentifier: Product_Exploratory
  orgIdentifier: default
  tags: {}
  properties:
    ci:
      codebase:
        connectorRef: account.tejakummarikuntla
        repoName: gh-actions-artifacts
        build: <+input>
  stages:
    - stage:
        name: Get Codebase
        identifier: Run_Lints
        description: ""
        type: CI
        spec:
          cloneCodebase: true
          platform:
            os: Linux
            arch: Amd64
          runtime:
            type: Cloud
            spec: {}
          caching:
            enabled: false
            paths: []
          slsa_provenance:
            enabled: false
          execution:
            steps:
              - step:
                  type: Run
                  name: Install dependencies
                  identifier: Install_dependencies
                  spec:
                    shell: Sh
                    command: npm ci
              - step:
                  type: Run
                  name: Lint code
                  identifier: Lint_code
                  spec:
                    shell: Sh
                    command: npm run lint
              - step:
                  type: Run
                  name: Test code
                  identifier: Test_code
                  spec:
                    shell: Sh
                    command: npm run test
    - stage:
        name: Install deps
        identifier: Install_deps
        description: ""
        type: CI
        spec:
          cloneCodebase: false
          platform:
            os: Linux
            arch: Amd64
          runtime:
            type: Cloud
            spec: {}
          execution:
            steps:
              - step:
                  type: Run
                  name: Install dependencies
                  identifier: Install_dependencies
                  spec:
                    shell: Sh
                    command: npm ci
    - stage:
        name: Lint code
        identifier: Lint_code
        description: ""
        type: CI
        spec:
          cloneCodebase: false
          platform:
            os: Linux
            arch: Amd64
          runtime:
            type: Cloud
            spec: {}
          execution:
            steps:
              - step:
                  type: Run
                  name: Lint check the code
                  identifier: Lint_check_the_code
                  spec:
                    shell: Sh
                    command: npm run lint
    - stage:
        name: Test code
        identifier: Test_code
        description: ""
        type: CI
        spec:
          cloneCodebase: false
          platform:
            os: Linux
            arch: Amd64
          runtime:
            type: Cloud
            spec: {}
          execution:
            steps:
              - step:
                  type: Run
                  name: Run code tests
                  identifier: Run_code_tests
                  spec:
                    shell: Sh
                    command: npm run test
